use Gamma version 1.0 on PC

AudioIn >> Level(gain: 1.5) >> AudioOut;

signal In[2] {
    default: [3,4.5]
}

signal Out[2] {
    default: [5,6.5]
}

In >> Out;

InSignal >> PassThru() >> OutSignal;

InSignal2 >> [PassThru(), PassThru()] >> OutSignal2;
