use Wiring version 1.0

import Generators

signal DownSample {
	rate: 1
}

signal Frequency {
	default: 2
	domain: WiringDomain
	}

#AnalogIn[0] * 4 >> DownSample >> SerialOut();
0.5 + (AnalogIn[0] /128.0) >> Frequency;

[Oscillator (frequency: Frequency) , 0 ] >> Greater() >> DigitalOut[13];

