# Frequency Modulation

use RtAudio version 1.0 on PC

import Generators

signal Modulator {
	rate:      441
	domain:    AudioDomain
}

Oscillator ( frequency: 0.5 amplitude: 100.0 ) >> Modulator;

Oscillator ( frequency: 440.0 + Modulator amplitude: 0.1 ) >> Pan (position: Modulator/100) >> AudioOut;
