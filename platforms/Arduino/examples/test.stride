use Arduino version 1.0 on Uno

import generators

signal DownSample {
	rate: 1
}

AnalogIn[1] * 4 >> DownSample >> SerialOut();
1 + AnalogIn[4] * (1.0/128.0) >> Frequency;

Oscillator(frequency: Frequency) > 0 >> DigitalOut[14];

