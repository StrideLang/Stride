# Frequency and Amplitude Modulation

use Gamma version 1.0 on PC

import generators

Oscillator ( frequency: [ 3.0, 5.0 ] amplitude: [ 40.0, 0.1 ] ) >> Modulator;

Oscillator ( frequency: 440.0 + Modulator[1] amplitude: Modulator[2] ) >> AudioOut;
