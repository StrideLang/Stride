use Gamma version 1.0 on PC

import generators

signal Frequency2 { 
	default: 440
}

reaction Test {
	streams: [
		Frequency2 + 50 >> Frequency2;
	]
	defaultDomain: AudioDomain
}

Impulse(frequency:1) == 1 >> Test();

Oscillator(frequency: Frequency2) >> Level(gain:0.2) >> AudioOut;

