use Gamma version 1.0 on PC

signal A[2] {}
signal B[2] {}
signal C {}
signal D[2] {}
signal E[2] {}
signal F[2] {}

A[1] >> B[2]; # One to one connection from bundles

C >> D; # C should be passed to both ports of D

E >> F; # This should be expanded

AudioIn >> AudioOut;

